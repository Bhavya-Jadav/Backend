// backend/routes/quizRoutes.js
const express = require('express');
const router = express.Router();

// Placeholder routes (extend as needed)
router.get('/', (_req, res) => res.json([]));

module.exports = router;

